# Provenance

This standalone skill's new instructions and `ppt_bridge.py` are MIT-licensed; see LICENSE.

The prior API-based PPT Translator (Copyright 2025 PPT Translator Contributors, MIT) informed the extraction/application separation. This bridge is a new local OOXML implementation, not a redistributed provider client. Preserve original notices if upstream source is imported later.

lxml, python-pptx, Pillow and local rendering applications are separate dependencies under their own licenses. Customer documents and provider credentials are not distributed.
